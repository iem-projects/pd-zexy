---
include:
  - https://git.iem.at/pd/iem-ci/raw/main/pd-lib-builder/iem-ci.yml

Linux:
  extends:
    - .Linux-amd64
    - .snapshot
  image: registry.git.iem.at/devtools/docker/debiancross:${arch}
  parallel:
    matrix:
      - arch: amd64
        pd_extension: l_amd64
      - arch: arm64
        pd_extension: l_arm64
#      - arch: armhf
#        pd_extension: l_arm


Darwin-fat:
  extends:
    - .Darwin
    - .snapshot

deken:
  extends:
    - .deken
    - .snapshot
